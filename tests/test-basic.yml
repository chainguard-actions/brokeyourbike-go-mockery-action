name: Test basic mockery setup
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - id: setup-mockery
        name: Setup mockery
        uses: .
        with:
          mockery-version: '2.43.2'
      - name: Assert mockery is in PATH
        run: |
          echo "Checking mockery is available in PATH..."
          if ! command -v mockery &> /dev/null; then
            echo "FAIL: mockery not found in PATH"
            exit 1
          fi
          echo "PASS: mockery found at $(command -v mockery)"
      - name: Assert mockery version
        run: |
          ACTUAL=$(mockery --version 2>&1 || true)
          echo "actual: $ACTUAL"
          if echo "$ACTUAL" | grep -q "2.43.2"; then
            echo "PASS: mockery version matches 2.43.2"
          else
            echo "FAIL: expected version 2.43.2 in output: $ACTUAL"
            exit 1
          fi
